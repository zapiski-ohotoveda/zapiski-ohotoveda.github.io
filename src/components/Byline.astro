---
// Author byline. A <div> (not <p>) so it never becomes the .prose first
// paragraph that receives the drop cap. Ready to become a link to the
// about-the-author page once it exists.
import { AUTHOR } from '../consts';
---
<div class="byline">{AUTHOR}</div>
